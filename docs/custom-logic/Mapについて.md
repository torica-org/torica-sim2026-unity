# Mapについて

## Layer
`Map`です．

`Camera`コンポーネントの`Culling Mask`で`Layer`ごとに表示を切り替えられます．

## Trail Renderer
`Resources`内の各機体の`Prefab`に含まれる`TPSCamera`には，
子オブジェクトとして`Trail Renderer`が含まれています．

`Trail Renderer`は，地図に表示する経路のためのオブジェクトを生成しています．
